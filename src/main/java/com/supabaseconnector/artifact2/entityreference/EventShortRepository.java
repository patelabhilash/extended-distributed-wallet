package com.supabaseconnector.artifact2.entityreference;

import java.time.LocalDateTime;
import java.util.List;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

/* @Repository
public interface EventShortRepository extends JpaRepository<EventShort, String> {
    
     List<EventShort> findByEventdate(LocalDateTime localDateTime);
}

 */